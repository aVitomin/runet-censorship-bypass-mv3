# Historical legacy options-bundle README

> Preserved from the upstream MV2 options source directory. These generic
> boilerplate commands are not the current repository development workflow.

---

# React + Flow + Webpack 2 Boilerplate with Babel

Switch branches for adding/removing Babel for JSX + Flowtype.

## Install

`yarn install` or `npm install --dev`

## Run

`yarn/npm run build`
